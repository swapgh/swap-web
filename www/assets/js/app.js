document.addEventListener("DOMContentLoaded", () => {
  window.Swap?.theme?.init();
  window.Swap?.cookieConsent?.init();
  window.Swap?.dropdowns?.init();
  window.Swap?.mobileNav?.init();
  window.Swap?.codeToggle?.init();
  window.Swap?.lightbox?.init();
  window.Swap?.footerAccordion?.init();
  window.Swap?.accountNav?.init();
});
